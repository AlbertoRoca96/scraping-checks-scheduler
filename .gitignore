# Node / Playwright
node_modules/
playwright-report/
test-results/
*.log

# Ignore only the root-level data dir (Actions outputs)
# Leading slash anchors to repo root; docs/data is NOT ignored.
# https://git-scm.com/docs/gitignore
/data/

# Make doubly sure docs/data is tracked even if a future rule ignores docs/**
!docs/
!docs/data/
!docs/data/**

# macOS
.DS_Store
